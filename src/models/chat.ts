export type Message = {
  sender: string;
  content: string;
};

export type UserInfo = {
  userId: number;
  userName: string;
};

export type ProductInfo = {
  productId: number;
  productName: string;
};

export type SellerInfo = {
  sellerId: number;
  shopName: string;
};
